# Maintainer: george <rpubaddr0 {at} gmail [dot] com>

pkgname=ddrescue
pkgver=1.16
pkgrel=1
pkgdesc='A data recovery tool which tries hard to rescue data in case of read errors.'
arch=('i686' 'x86_64')
url='http://www.gnu.org/software/ddrescue/ddrescue.html'
license=('GPL3')
source=("http://ftpmirror.gnu.org/ddrescue/${pkgname}-${pkgver}.tar.gz")
sha256sums=('76b3f2e5fb0306d24f2632c3e168cccb73dc0a348e3a7089cd9230748ff23de6')

build() {
	cd "${srcdir}/${pkgname}-${pkgver}"
	./configure --prefix=/usr
	make
}

package() {
	cd "${srcdir}/${pkgname}-${pkgver}"
	make DESTDIR="$pkgdir" install
}
